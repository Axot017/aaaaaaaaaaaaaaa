import 'package:flutter/material.dart';

class AppAnimationInfo {
  static const LOGIN_ANIMATION_DURATION = const Duration(milliseconds: 750);
  static const IMAGE_ANIMATION_DURATION = const Duration(milliseconds: 300);
  static const LIST_ANIMATION_DURATION = const Duration(milliseconds: 250);
  static const BASE_ANIMATION_DURATION = const Duration(milliseconds: 500);
  static const BASE_ANIMATION_CURVE = Curves.easeOutCubic;
  static const LOGIN_ANIMATION_CURVE = Curves.easeOutQuint;
  static const IMAGE_ANIMATION_CURVE = Curves.easeOutQuad;
}